<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <title>Contato - E-ncarte</title>
</head>
<body>
    <%- include('partials/menu') %>
        <div id="area-central" class="container-fluid">
            <div class="row">
                <div class="col-12 col-sm-10 col-lg-8 col-xl-6 mx-auto mt-5">
                    <h3 class="mb-5">Envie sua mensagem para o E-ncarte</h3>
                    <form action="/contato" method="POST">
                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                                <span class="input-group-text">Nome</span>
                            </div>
                            <input name="nome" class="form-control" type="text">
                        </div>
                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                                <span class="input-group-text">E-mail</span>
                            </div>
                            <input name="email" class="form-control" type="text">
                        </div>
                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                                <span class="input-group-text">Assunto</span>
                            </div>
                            <input name="assunto" class="form-control" type="text">
                        </div>                    
                        <div>Mensagem</div>
                        <textarea name="mensagem" class="d-block form-control" cols="30" rows="5"></textarea>
                        <button class="btn btn-info mt-2" type="submit">Enviar</button>
                    </form>
                    <% if (mensagemEnviada) { 
                        if (!erro) {%>
                            <div class="alert alert-success alert-dismissible fadeshow" role="alert">
                                <strong>Mensagem enviada!</strong><%= mensagem %>
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                        <%}
                        else {%>
                            <div class="alert alert-danger alert-dismissible fadeshow" role="alert">
                                <strong>Erro ao enviar mensagem :(</strong> <%= mensagem %>
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                        <% }                        
                    } %>                    
                </div>
                
            </div>
        </div>
    <%- include('partials/footer') %>     
    <%- include('partials/bootstrapjs') %>
</body>
</html>