<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('../partials/head')%>
    <%-include('../partials/bootstrapjs')%>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nova Opção Personalizada - e-ncarte</title>
</head>
<body class="bg-light">
    <%-include('../partials/menu')%>  
    <div class="container-fluid">
        <div class="row">
            <div class="col-4">
                <a href="/admin/editarcampanhacuponsativa?idCliente=<%= cliente._id %>&idCampanha=<%= campanha._id %>"><i class="fas fa-long-arrow-alt-left fa-3x"></i></a>
                <h3>Nova opção personalizada</h3>
                <form class="mt-3" action="/cupons/novaopcaopersonalizada" method="POST">
                    <input type="text" class="d-none" name="idCliente" value="<%= cliente._id %>">
                    <input type="text" class="d-none" name="idCampanha" value="<%= campanha._id %>">

                    <div class="form-group">
                        <h5>Descrição</h5>
                        <input id="descricao" type="text" name="descricao" class="form-control">
                    </div>
                    <div class="form-group">
                        <h5>Quantidade disponível</h5>
                        <input id="descricao" type="number" name="quantidadeDisponivel" class="form-control w-50" value="0" min="0">
                    </div>
                    <input class="btn btn-success" type="submit" value="salvar">
                </form>
            </div>
        </div>
    </div>
</body>
</html>